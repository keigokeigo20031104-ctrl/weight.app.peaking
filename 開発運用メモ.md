# 開発運用メモ

## アプリ生成時の標準方針

- アプリの生成や更新は Cloudflare Pages / Workers へ置く前提で扱う。
- スマホから確認しやすいURLを最後に提示する。
- Cloudflareへ置く場合は、静的ファイルだけで済むか、サーバーAPIやKV bindingが必要かを先に整理する。
- ドラッグ&ドロップデプロイでAPIが必要な場合は、`functions/` だけに頼らず `_worker.js` 形式も用意する。
- KV Binding名は `WEIGHT_LOGS` のままにする。
